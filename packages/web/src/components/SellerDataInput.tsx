import { Wrapper } from "../sharedCompnents/Wrapper";
import { Context } from "../sharedCompnents/Context";
import { Container } from "../sharedCompnents/NavBarStyle";

const SellerDataInput = () => {
  return (
    <Wrapper style={{ flexDirection: "row" }}>
      <Container
        style={{ backgroundColor: "blue", width: "8vw", height: "100vh" }}
      >
        fsdfsdfsdf
      </Container>
      <Context style={{ width: "80vw", alignSelf: "right" }}>gdfgsdgs</Context>
    </Wrapper>
  );
};

export default SellerDataInput;
